mixin review(review)
  .review__header
    .review__author
      img.avatar(src=review.author.gravatar, alt="")
      p= review.author.name
    .review__stars(title=`Rating ${review.rating} out of 5 stars!`)
      = `⭐`.repeat(review.rating)
      = `👎`.repeat(5 - review.rating)
    time.review__time(datetime=review.created)= h.moment(review.created).fromNow()
  .review__body
    p= review.text